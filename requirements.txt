# Core web stack
fastapi
uvicorn
httpx

# Frontend / UI
textual[syntax]

# Trading / Charts
ccxt
lightweight-charts

# Data & Utils
polars
python-dotenv

# AI / LLM
agno
google-genai
ollama

# Indicators / Quant
ta-lib  # Requires system-level TA-Lib installed

# Vector / Search
pinecone

# Database drivers (choose ONE psycopg variant)
psycopg[binary]  # Modern psycopg (v3) binary wheels
asyncpg  # For async PostgreSQL access
